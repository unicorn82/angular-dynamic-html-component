import { Directive2Directive } from './directive2.directive';

describe('Directive2Directive', () => {
  it('should create an instance', () => {
    const directive = new Directive2Directive();
    expect(directive).toBeTruthy();
  });
});
